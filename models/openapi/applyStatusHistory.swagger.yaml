openapi: 3.0.0
info:
  title: Converted from applyStatusHistory.swagger.yaml with typeconv
  version: '1'
  x-id: applyStatusHistory.swagger.yaml
  x-comment: >-
    Generated from models\applyStatusHistory.ts by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    ApplyStatusHistory:
      properties:
        id:
          title: ApplyStatusHistory.id
          type: string
        apply:
          $ref: '#/components/schemas/Apply'
          title: ApplyStatusHistory.apply
        status:
          $ref: '#/components/schemas/ApplyHistoryStatus'
          title: ApplyStatusHistory.status
        createdAt:
          title: ApplyStatusHistory.createdAt
          type: string
      required:
        - id
        - apply
        - status
        - createdAt
      additionalProperties: false
      title: ApplyStatusHistory
      type: object
