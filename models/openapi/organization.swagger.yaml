openapi: 3.0.0
info:
  title: Converted from organization.swagger.yaml with typeconv
  version: '1'
  x-id: organization.swagger.yaml
  x-comment: >-
    Generated from models\organization.ts by core-types-json-schema
    (https://github.com/grantila/core-types-json-schema) on behalf of typeconv
    (https://github.com/grantila/typeconv)
paths: {}
components:
  schemas:
    Organization:
      properties:
        id:
          title: Organization.id
          type: string
        name:
          title: Organization.name
          type: string
        contractAddress:
          title: Organization.contractAddress
          type: string
      required:
        - id
        - name
        - contractAddress
      additionalProperties: false
      title: Organization
      description: |-
        @swagger components:
         schemas:
           Organization:
             type: object
             properties:
               id:
                 type: string
                 format: cuid
               name:
                 type: string
               contractAddress:
                 type: string
      type: object
